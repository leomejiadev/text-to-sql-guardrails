server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing: cualquier ruta que no sea un archivo estático carga index.html
    # Sin esto, al refrescar /dashboard daría 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}
